var type = {
};

relation
  property: [
   [ parent | child ],
   [ description | introductory | procedural ]

  get
  set
  del

  importance  number
  correlation weak -> strong

node
  
  content
  prop:
    [ category | inline | block ]
    category - rectangles
    inline - within content
    block - undecorated

  subject const -> color
  relation: *

  
horizontal lines are categories

description - property or attribute it's in a fragment
  vertical properties represent the linguistic flow


boxes are different from background color

nodes:
  if not linked, not decorated
  introductory - straight
  description - slanted ones
  categorical
  plain boring textual node that is like a category but isn't
  all nodes can be linked


edges:
  have titles
  procedural
  aside


length of line is meaningless

color inheritance


content:
  circles
  terms
  topics (circles)
  cross-references they can be topically classified (through colors) arbitrarily

y-connectors
  categorical relationship (horizontal) ... those are nodes
